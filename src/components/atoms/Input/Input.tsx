import React from 'react'

export const Input = () => {
  return <div>Input</div>
}

/* export interface InputPropos {
  href?: string
  onChange? :
}
export const Input: FC<InputPropos> =({ placeholder, functionSet, role }) {
  let type = ''
  if (role == 'password') {
    type = 'password'
  } else {
    type = 'text'
  }
  return (
    <input
      value={Submit}
      type={type}
      placeholder={placeholder}
      onChange={(e) => functionSet(e.target.value)}
    ></input>
  )
} */
